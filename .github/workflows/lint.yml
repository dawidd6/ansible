name: Lint

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install linter
        run: pip install ansible ansible-lint

      - name: Run linter
        run: ansible-lint -p
