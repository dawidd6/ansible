name: Lint

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install linter
        run: |
          sudo apt update
          sudo apt install ansible-lint

      - name: Lint
        run: ansible-lint *.yml
