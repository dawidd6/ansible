- name: Setup
  hosts: localhost
  roles:
    - { role: apt, tags: apt }
    - { role: brew, tags: brew }
    - { role: flatpak, tags: flatpak }
    - { role: dotfiles, tags: dotfiles }
    - { role: scripts, tags: scripts }
    - { role: keys, tags: keys }
    - { role: mods, tags: mods }
